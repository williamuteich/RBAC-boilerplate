export default function AdminDashboardPage() {
    return (
        <div className="flex flex-col items-center justify-center w-full min-h-[60vh] animate-in fade-in duration-500">
            <h1 className="text-2xl font-semibold text-muted-foreground">Admin Dashboard</h1>
            <p className="text-muted-foreground mt-2">Área de trabalho limpa para iniciar o desenvolvimento.</p>
        </div>
    );
}
